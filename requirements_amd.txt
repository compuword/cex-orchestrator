# CEX Orchestrator -- AMD Radeon requirements
# Covers: ONNX DirectML inference + Vulkan NeRF (torch-ngp) capabilities
#
# Minimum GPU: RX 6600 (RDNA2, DirectML 1.8+)
# Recommended: RX 6800 XT / RX 7900 XT (16GB+ VRAM)
# Windows driver: Adrenalin 23.7.1+  (DirectML 1.13)
# Linux driver:   ROCm 5.7+ (Ubuntu 22.04 recommended)
# Vulkan SDK: 1.3+ (https://vulkan.lunarg.com/)

# --- Core inference (local ONNX via DirectML, no cloud) ---
numpy>=1.24.0
onnx>=1.14.0
onnxruntime-directml>=1.17.0     # DirectML EP: Windows AMD RX 6000+ and NVIDIA
pyyaml>=6.0

# --- torch-ngp / Vulkan NeRF (AMD-compatible NeRF alternative to instant-ngp) ---
# torch-ngp: https://github.com/ashawkey/torch-ngp
# Works on AMD via:
#   Windows: torch + torch-directml
#   Linux:   torch + ROCm
#
# Windows AMD (DirectML):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-directml
#
# Linux AMD (ROCm 5.7):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm5.7

# Core ML / NeRF stack
commentjson>=0.9.0               # JSON with comments (ngp-style configs)
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
opencv-python-headless>=4.8.0
pybind11>=2.11.0                 # for custom extension builds
pyquaternion>=0.9.9              # camera quaternion math
scipy>=1.11.0
tqdm>=4.66.0

# torch-ngp specific
trimesh>=4.0.0                   # 3D mesh loading/export
dearpygui>=1.9.0; platform_system == "Windows"  # GUI viewer (optional)

# --- OpenEXR (HDR images) ---
# Same as RTX -- install pre-built wheel for Windows:
#   pip install OpenEXR-1.3.2-cp39-cp39-win_amd64.whl  (Python 3.9)
# Linux: pip install openexr (with libopenexr-dev)

# --- COLMAP (Structure-from-Motion) ---
pycolmap>=0.6.0; platform_system != "Windows"
# Windows: install COLMAP binary from https://github.com/colmap/colmap/releases

# --- Vulkan (AMD compute via RDNA Vulkan driver) ---
# AMD RDNA2+ has excellent Vulkan 1.3 support
# Install Vulkan SDK: https://vulkan.lunarg.com/sdk/home#windows
# AMD's Vulkan driver (AMDVLK) or Mesa RADV (Linux)

# --- AMD GPU monitoring ---
# Windows: AMD does not expose VRAM via pynvml; use DirectML device query
# Linux: rocm-smi (installed with ROCm driver stack)
psutil>=5.9.0
gputil>=1.4.0                    # generic GPU polling (fallback)

# --- Server/network ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
